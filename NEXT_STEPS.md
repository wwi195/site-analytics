# 今日やること（NEXT_STEPS）

コードは GitHub に push 済みです: https://github.com/wwi195/site-analytics

この後は **Google と GitHub の画面上での操作**のみです。以下の順番どおりに進めてください。
細かい注意点や「うまくいかない時」の対処は `README.md` にまとめてあるので、
詰まったらそちらも見てください。このファイルは「今日、何をどの順でクリックするか」だけに絞っています。

**所要時間の目安: 40〜50分**（Googleの承認画面や、GitHub Pagesの反映待ちの時間を含みます）

---

## この後の流れ（全体像）

1. Googleスプレッドシートを作る（2分）
2. GASプロジェクト「collector」を作る・公開する（8分）
3. GASプロジェクト「dashboard」を作る・公開する（12分）
4. GitHub Pagesを有効にする（5分）
5. `tracker.js` の送信先を書き換えて再pushする（5分）
6. 実際のサイトに計測タグを埋め込む（3分）
7. 動作確認する（5分）

**このガイドの中で、「ここで控えた値」を後の手順で貼り付ける場面が3回あります。**
控える場面には 📋、貼り付ける場面には 📌 の印をつけています。

---

## 手順1: Googleスプレッドシートを作る（2分）

1. ブラウザで https://drive.google.com を開く。
2. 左上の「＋ 新規」ボタンをクリック → 「Google スプレッドシート」をクリック。
3. 開いたスプレッドシートの左上のタイトル（「無題のスプレッドシート」）をクリックして、
   `site-analytics-data` のような名前に変更する。
4. ブラウザのアドレスバーのURLを見る。
   `https://docs.google.com/spreadsheets/d/【この部分】/edit` の
   【この部分】（英数字とハイフンの長い文字列）をコピーする。

   📋 **控える値①: スプレッドシートID**（手順2と手順3の両方で使います）

---

## 手順2: GASプロジェクト「collector」を作る・公開する（8分）

これは「各サイトから計測データを受け取る」ための窓口です。

1. ブラウザで https://script.google.com を開く。
2. 「新しいプロジェクト」ボタンをクリック。
3. 左上のプロジェクト名（「無題のプロジェクト」）をクリックして
   `site-analytics-collector` に変更し、Enterを押す。
4. 中央のコードエディタに表示されている `コード.gs` の中身を全選択（Ctrl+A）して削除する。
5. このリポジトリの `collector.gs` の中身を全部コピーして、エディタに貼り付ける。
6. 貼り付けたコードの1行目付近にある `SPREADSHEET_ID = 'PUT_YOUR_SPREADSHEET_ID_HERE'` の
   `PUT_YOUR_SPREADSHEET_ID_HERE` の部分を、

   📌 **控えた値①（スプレッドシートID）に書き換える**

7. 2行目の `ALLOWED_SITE_IDS = ['portfolio']` の `'portfolio'` を、計測したい自分のサイトの
   識別名（好きな英数字、例: `'my-blog'`）に書き換える。複数サイトある場合は
   `['my-blog', 'my-game']` のようにカンマ区切りで増やせる。
8. Ctrl+S で保存する。
9. 右上の「デプロイ」ボタン → 「新しいデプロイ」をクリック。
10. 「種類の選択」の歯車アイコンをクリックし、「ウェブアプリ」を選ぶ。
11. 「次のユーザーとして実行」は「自分」のままでよい。
12. 「アクセスできるユーザー」を **「全員」** に変更する（ここ重要）。
13. 「デプロイ」ボタンをクリックする。
14. 「このアプリは Google で確認されていません」という警告画面が出たら、これは自分だけが
    使う個人スクリプトなので正常な表示。「詳細」をクリック →
    「（プロジェクト名）（安全ではないページ）に移動」をクリック → 通常の権限確認画面が出るので
    「許可」をクリックする。
15. デプロイが完了すると「ウェブアプリ」の欄にURL
    （`https://script.google.com/macros/s/.../exec` の形式）が表示される。これをコピーする。

    📋 **控える値②: collectorのウェブアプリURL**（手順5で使います）

---

## 手順3: GASプロジェクト「dashboard」を作る・公開する（12分）

これは「自分だけが見られる集計画面」です。**collectorとは別の、新しいプロジェクト**として作ること。

1. もう一度 https://script.google.com を開き、「新しいプロジェクト」をクリック
   （手順2で開いたcollectorのタブとは別に、新規で作る）。
2. プロジェクト名を `site-analytics-dashboard` に変更する。
3. `コード.gs` の中身を全部削除し、このリポジトリの `dashboard.gs` の中身を貼り付ける。
4. `SPREADSHEET_ID = 'PUT_YOUR_SPREADSHEET_ID_HERE'` の部分を、

   📌 **控えた値①（スプレッドシートID）に書き換える**（手順2と同じ値）

5. 左側のファイル一覧の「+」アイコン →「HTML」をクリックし、ファイル名の欄に `dashboard`
   と入力してEnter（拡張子`.html`は自動でつく）。
6. 作成された `dashboard.html` の中身を全部削除し、このリポジトリの `dashboard.html` の
   中身を貼り付ける。
7. Ctrl+S で保存する。
8. ツールバー中央の「実行する関数を選択」というプルダウンから `setup` を選び、
   左にある「実行」ボタン（▷アイコン）をクリックする。
9. 手順2-14と同じ「Google で確認されていません」の確認画面が出たら、同じ手順で進めて許可する
   （このプロジェクトでは初回のみ）。
10. 実行が終わったら、手順1で作ったスプレッドシートを開き直し、`raw` と `daily` という
    2つのシートが増えていて、それぞれ1行目に見出し（date, timestamp, siteId…など）が
    入っていることを確認する。
11. プルダウンを `createDailyTrigger` に変えて、また「実行」ボタンをクリックする
    （これで毎日自動集計されるようになる）。
12. 右上の「デプロイ」→「新しいデプロイ」→ 歯車アイコンで「ウェブアプリ」を選ぶ。
13. 「次のユーザーとして実行」は「自分」のまま。
14. 「アクセスできるユーザー」を **「自分のみ」** にする（collectorと逆にする。ここ重要）。
15. 「デプロイ」をクリックする。表示されたURLが**ダッシュボードのURL**。
    ブラウザのブックマークに追加しておく（毎日この画面を見ることになる）。

---

## 手順4: GitHub Pagesを有効にする（5分）

コードはもうGitHubにpush済みなので、公開設定をオンにするだけです。

1. ブラウザで https://github.com/wwi195/site-analytics を開く。
2. 上部タブの「Settings」をクリック（歯車アイコンの隣）。
3. 左メニューの「Pages」をクリック。
4. 「Build and deployment」の「Source」が「Deploy from a branch」になっていることを確認する。
5. その下の「Branch」のプルダウンで `main` を選び、隣のフォルダ指定は `/ (root)` のまま、
   「Save」ボタンをクリックする。
6. 数分待つと、同じ画面の上部に
   `https://wwi195.github.io/site-analytics/` という公開URLが表示される
   （表示されるまで1〜2分かかることがある。出なければページを再読み込みする）。

---

## 手順5: `tracker.js` の送信先を書き換えて再pushする（5分）

このリポジトリを普段どおりの方法（エディタやGit）で開き、以下を行う。

1. `tracker.js` の1行目付近にある

   ```js
   var ENDPOINT = 'https://script.google.com/macros/s/PUT_COLLECTOR_DEPLOYMENT_ID_HERE/exec';
   ```

   の `PUT_COLLECTOR_DEPLOYMENT_ID_HERE/exec` を含むURL全体を、

   📌 **控えた値②（collectorのウェブアプリURL）に書き換える**

2. 変更を保存し、`git add tracker.js`、`git commit -m "chore: set collector endpoint URL"`、
   `git push` を実行する。
3. 1〜2分待ってから、ブラウザで
   `https://wwi195.github.io/site-analytics/tracker.js?v=1` を開き、
   書き換えた `ENDPOINT` の値が反映されていることを確認する。

---

## 手順6: 実際のサイトに計測タグを埋め込む（3分）

計測したい自分のサイト（例: ポートフォリオサイト）のHTMLの `</body>` の直前に、
以下を1行追加する。

```html
<script src="https://wwi195.github.io/site-analytics/tracker.js?v=1" data-site-id="my-blog"></script>
```

`data-site-id` の値は、手順2-7で `ALLOWED_SITE_IDS` に登録した値と**完全に一致**させること。
追加したらそのサイトを再公開（再デプロイ）する。

---

## 手順7: 動作確認する（5分）

1. 手順6で埋め込んだサイトをブラウザで開く。
2. スプレッドシート（手順1）の `raw` シートを開き、1行データが増えていることを確認する
   （増えるまで数秒〜数十秒かかることがある）。
3. その日のうちに正しく動いているか手っ取り早く見たい場合は、`README.md` の
   「動作確認手順」にある `getTodayStats` の手動実行方法を参照する。
4. 手順3-15でブックマークした**ダッシュボードURL**を開き（自分のGoogleアカウントで
   ログインした状態で）、サイト選択プルダウン・表・折れ線グラフが表示されることを確認する。
   データが集計されるのは翌日以降（`aggregateDaily` が毎日実行された後）なので、
   初日はグラフが空でも問題ない。当日分の数字は表の一番下の行に速報値として出る。

---

## 困ったときは

- Google/GASの詳しい操作や、うまくいかない時の対処は `README.md` を参照。
- 新しいサイトを追加したくなったら: `collector.gs` の `ALLOWED_SITE_IDS` に追加 →
  再デプロイ（デプロイ→デプロイを管理→編集アイコン→新バージョン→デプロイ）を忘れずに。
